@{
    ViewData["Title"] = "Проверьте вашу почту";
}

<div class="row">
    <div class="col-md-6 offset-md-3">
        <div class="card">
            <div class="card-body text-center">
                <div class="mb-4">
                    <i class="fas fa-envelope-open-text fa-4x text-primary"></i>
                </div>

                <h2 class="card-title">@ViewData["Title"]</h2>

                <div class="alert alert-success mt-4">
                    <p class="mb-0">
                        Если адрес <strong>@Context.Request.Query["email"]</strong> зарегистрирован в системе,
                        вы получите письмо с инструкциями по сбросу пароля.
                    </p>
                </div>

                <div class="mt-4">
                    <a asp-action="Login" class="btn btn-primary">
                        <i class="fas fa-sign-in-alt mr-2"></i>Вернуться к входу
                    </a>
                </div>

                <div class="mt-3 text-muted small">
                    Не получили письмо? Проверьте папку "Спам" или
                    <a asp-action="ForgotPassword">попробуйте снова</a>.
                </div>
            </div>
        </div>
    </div>
</div>